<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interview Rounds</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- FontAwesome Icons -->
    <style>
        body {
            background-image: url('re.jpg'); /* Background image */
            background-size: cover;
            background-position: center;
            background-color:white; /* Dark background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Arial', sans-serif;
            color: white;
            overflow: hidden;
        }

        .container {
            background: rgba(0, 0, 0, 0.7);  /* Semi-transparent container */
            padding: 40px 60px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8);
            text-align: center;
            backdrop-filter: blur(10px); /* Blur effect */
            opacity: 0; /* Initially hidden */
            animation: moveIn 2s forwards; /* Animation to move in */
        }

        .header {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 30px;
            letter-spacing: 2px;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
            opacity: 0;
            animation: fadeIn 1s 1.5s forwards; /* Fade in with delay */
        }

        .info-container {
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeIn 1s 2s forwards; /* Fade in with delay */
        }

        .info-container p {
            font-size: 20px;
            color: #fff;
            margin: 10px 0;
        }

        .button-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            justify-content: center;
            align-items: center;
            opacity: 0; /* Initially hidden */
            animation: fadeIn 1s 3s forwards; /* Fade in with delay */
        }

        .button {
            background: linear-gradient(45deg, #6a1b9a, #ff4081); /* Gradient color for the button */
            color:white;
            border: none;
            padding: 20px;
            margin: 10px;
            width: 300px;
            cursor: pointer;
            font-size: 18px;
            border-radius: 15px;
            transition: transform 0.3s, box-shadow 0.3s, background-color 0.3s;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .button:hover {
            background-color: #ff4081;  /* Lighter color on hover */
            transform: scale(1.05);
            box-shadow: 0 0 15px rgba(255, 64, 129, 0.8);  /* Glow effect */
        }

        .button i {
            margin-right: 15px;
            font-size: 24px;
            transition: transform 0.3s;
        }

        .button:hover i {
            transform: rotate(10deg);  /* Rotate icon on hover */
        }

        .footer {
            margin-top: 30px;
            font-size: 14px;
            color: #ddd;
        }

        .footer a {
            color: #ff4081;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer a:hover {
            color: #fff;
            text-decoration: underline;
        }

        .info-icon {
            font-size: 48px;
            color: #ff4081;
            margin-bottom: 20px;
        }

        /* Keyframe animations */
        @keyframes moveIn {
            from {
                transform: translateX(-100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        a {
            text-decoration: none;
        }

    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <i class="fas fa-bullhorn info-icon"></i>
            Welcome to the Interview Rounds
        </div>
        
        <!-- Info Container with moving text -->
        <div class="info-container">
            <p>Get ready for three exciting rounds: Aptitude, Technical, and HR.</p>
            <p>Each round is designed to assess your skills and knowledge.</p>
            <p>Let’s get started and show your potential!</p>
        </div>

        <div class="button-container">
            <a href="/Apti">
                <button class="button">
                    <i class="fas fa-calculator"></i> Aptitude Round
                </button>
            </a>
            
            <a href="/Tech">
                <button class="button">
                    <i class="fas fa-laptop-code"></i> Technical Round
                </button>
            </a>
            <a href="/chatbot">
            <button class="button">
                <i class="fas fa-users"></i> HR Round
            </button>
            </a>
        </div>

        
    </div>
</body>
</html>
